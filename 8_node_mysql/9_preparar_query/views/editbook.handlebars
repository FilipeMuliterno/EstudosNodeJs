<h1>Edite o seu livro:</h1>
<form action="/books/updatebook" method="POST" class="form">
  {{! esse input hiden serve para voce passar o id do projeto no post junto }}
  <input type="hidden" name="id" value="{{book.id}}" />
  <div class="form-control">
    <a href="/books">Livros Cadastrados</a>
    <br />
    <label for="title">Título:</label>
    <input
      type="text"
      name="title"
      placeholder="Título do livro"
      value="{{book.title}}"
    />
  </div>
  <div class="form-control">
    <label for="pagesqty">Quantidade de páginas:</label>
    <input
      type="number"
      name="pageqty"
      placeholder="Quantidade de páginas"
      value="{{book.pageqty}}"
    />
  </div>
  <input type="submit" value="Editar" />
</form>